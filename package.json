{
  "name": "ninja-universe-fighters",
  "version": "0.6.0",
  "private": true,
  "type": "module",
  "description": "Offline mobile ninja arena fighting game. No build step — this file only exists so Node can run the validation scripts in tests/ as ES modules.",
  "scripts": {
    "test": "node tests/run-all.js"
  }
}
